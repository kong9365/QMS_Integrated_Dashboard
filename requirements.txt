# QMS Integrated Dashboard - Python 의존성
# Python 3.13 기준. 운영 재현성을 위해 핵심 패키지는 == 로 핀(Task 0.2).
# 측정 환경: Python 3.13.5 (사양서 Phase 0 베이스라인 docs/BASELINE.md 참조)

# 웹 UI
# 1.44.1: st.dialog / st.segmented_control 지원(Phase 2 IA 재편에 사용)
streamlit==1.44.1

# 데이터 처리
pandas==2.2.3
numpy==2.4.2

# 시각화
plotly==6.0.1

# HTTP / API
requests==2.32.5
urllib3==2.0.4

# Excel 내보내기 (pd.ExcelWriter)
openpyxl==3.1.5

# 선택: PyArrow (pandas 가 Parquet/Arrow 사용 시)
pyarrow==19.0.1

# PDF 보고서 (qms_pdf_report.py)
fpdf2==2.8.7
